# Nada de esto se publica jamas.
.claude/
secrets.yaml
*.token
credenciales*.json
*.backup
node_modules/

# Las capturas SI van al repositorio: fueron revisadas una por una y la de Inicio tiene
# tapado el bloque de presencia. Lo que no entra es cualquier imagen nueva sin revisar.
#
# Antes de agregar una: mirarla. Nombres, la calle, IPs, y las caratulas de Plex.
